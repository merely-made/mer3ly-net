# Organization-transfer receipts

One reviewed receipt belongs here for each repository transferred into the
`merely-made` organization.

M0 creates only this routing file. A repository receipt is added after its M1
publication gate and explicitly authorized M2 transfer. Receipts record
results and hashes, never credential values or private machine paths.
